<%= form_with model: [@article, @article.comments.build] do |f| %>
    <%= f.label :commenter %>
    <%= f.text_field :commenter %>
    <br />
    <%= f.label :body %>
    <%= f.text_field :body %>
    <br />
    <%= f.submit %>
<% end %>